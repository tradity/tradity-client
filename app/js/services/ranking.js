'use strict';

/**
 * @ngdoc service
 * @name tradity.ranking
 * @description
 * # handels the ranking group/user etc
 * Factory
 */
angular.module('tradity')
	.factory('ranking', function ($q) {
		return {
		};
	});
