import { Component } from '@angular/core';

@Component({
  moduleId: module.id,
  selector: 'tradity-icon',
  styleUrls: ['icon.component.css'],
  template: '<ng-content></ng-content>'
})
export class IconComponent { }